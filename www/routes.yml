/user/add:
  controller: "User"
  action: "register"

/se-connecter:
  controller: "User"
  action: "login"

/se-deconnecter:
  controller: "User"
  action: "logout"

/cree-un-compte:
  controller: "User"
  action: "register"

/backoffice:
  controller: "Backoffice"
  action: "dashboard"

/page:
  controller: "page"
  action: "show"
